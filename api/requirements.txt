# Floor constraints rather than exact pins: this project runs on Python 3.14,
# where some older wheels do not exist. Pin exactly once CI is green.
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg[binary]>=3.2.10
pydantic[email]>=2.10.4
pydantic-settings>=2.7.0
pyjwt>=2.10.1
passlib>=1.7.4
bcrypt>=4.2.1
python-multipart>=0.0.20
structlog>=24.4.0
Faker>=33.1.0

# Layer 2 — forecasting. Holt-Winters and the backtest harness only; there is
# no deep-learning stack here on purpose (see app/ai/forecasting/service.py).
numpy>=2.1.0
statsmodels>=0.14.4

# dev / test
pytest>=8.3.4
httpx>=0.28.1
ruff>=0.8.4
